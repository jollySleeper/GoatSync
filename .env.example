# GoatSync Environment Variables
# Copy this file to .env and customize values
#
# cp .env.example .env
# chmod 600 .env

# ═══════════════════════════════════════════════════════════
# REQUIRED - Must be set for production
# ═══════════════════════════════════════════════════════════

# PostgreSQL connection string (required)
# Format: postgres://user:password@host:port/database?sslmode=disable
DATABASE_URL=postgres://goatsync:change_me@postgres:5432/goatsync?sslmode=disable

# Encryption secret for login challenge encryption (required, min 32 characters)
# Generate with: openssl rand -base64 32
ENCRYPTION_SECRET=change_this_to_a_secure_32_char_key_or_longer

# ═══════════════════════════════════════════════════════════
# OPTIONAL - Server Settings
# ═══════════════════════════════════════════════════════════

# Server port (default: 3735)
# PORT=3735

# Enable debug mode (default: false)
# DEBUG=false

# Gin web framework mode: debug, release, test (default: release)
# GIN_MODE=release

# ═══════════════════════════════════════════════════════════
# OPTIONAL - Redis (for real-time WebSocket sync)
# ═══════════════════════════════════════════════════════════

# Redis URL for WebSocket pub/sub (optional but recommended)
# REDIS_URL=redis://redis:6379/0

# ═══════════════════════════════════════════════════════════
# OPTIONAL - Security Settings
# ═══════════════════════════════════════════════════════════

# How long login challenges are valid in seconds (default: 300)
# CHALLENGE_VALID_SECONDS=300

# CORS allowed origins, comma-separated (default: *)
# ALLOWED_ORIGINS=https://your-domain.com

# Allowed Host headers, comma-separated (default: *)
# ALLOWED_HOSTS=your-domain.com,www.your-domain.com

# ═══════════════════════════════════════════════════════════
# OPTIONAL - Storage Paths
# ═══════════════════════════════════════════════════════════

# Chunk storage directory - stores encrypted binary data (calendar/contact content)
# This is the only storage path currently used. A Docker volume is attached
# at /data/chunks in the provided docker-compose files.
# Default: ./data/chunks (local) or /data/chunks (Docker)
# CHUNK_STORAGE_PATH=/data/chunks

# ═══════════════════════════════════════════════════════════
# ALTERNATIVE - Individual Database Settings
# ═══════════════════════════════════════════════════════════
# Use these instead of DATABASE_URL if preferred

# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=
# DB_NAME=goatsync
# DB_SSLMODE=disable
